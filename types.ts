/* import type { Locale } from "./path-to-locale"

export interface SearchParams {
    searchParams: Promise<{
        query?: string
        filter?: string
        page?: string
    }>
    params: {
        locale: Locale
    }
} */